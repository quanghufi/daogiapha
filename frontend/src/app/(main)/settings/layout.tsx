import type { ReactNode } from 'react';

export const metadata = {
  title: 'Cài đặt tài khoản | AncestorTree',
};

export default function SettingsLayout({ children }: { children: ReactNode }) {
  return children;
}
